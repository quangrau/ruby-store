<div class="head-box">
	<div class="container">
		<h1>Editting category</h1>
		<h3>Update category info in store</h3>
	</div>
</div>
<div class="container" id="main">
<%= form_for @category, url: admin_category_path, :html => {:class => 'col-md-6'}  do|f| %>
  <% if @category.errors.any? %>
    <div class="alert alert-dagger">
        <h2><%= pluralize(@category.errors.count, "error") %> prohibited
          this category from being saved:</h2>
        <ul>
        <% @category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
 
  <div class="form-group">
    <%= f.submit nil, :class => 'btn btn-success' %>
  </div>
<% end %>
</div>